long long reversedBits(long long x) {
        // code here
        long long ans = 0,i=31;
        while(x){
            ans+=(long long)pow(2,i)*(x&1);
            i--;
            x>>=1;
        }
        return ans;
    }
